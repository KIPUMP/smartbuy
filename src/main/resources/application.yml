spring :
  application :
    name : demo

  datasource:
    url: jdbc:postgresql://localhost:5432/smartbuy
    username: user
    password: 0000
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        show_sql: true
    open-in-view: false

  server:
    port: 8080
